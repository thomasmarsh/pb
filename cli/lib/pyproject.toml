[project]
name = "pb-lib"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "sqlglot>=30.11.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pb"]
